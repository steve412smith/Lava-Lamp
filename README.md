# Lava Drift v2.2

Slow-controls test build.

Visible changes:
- Adds a visible `v2.2 slow-controls test` label under the title so you can confirm the new build loaded.
- Motion is reduced dramatically at the low end.
- Density max is much lower.
- Glow is capped to avoid whiteout.
- Service worker now tries network first to reduce stale GitHub Pages caching.

After uploading, test with `?v=22` at the end of the GitHub Pages URL.
